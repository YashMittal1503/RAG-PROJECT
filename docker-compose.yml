services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: doctalk-backend
    restart: unless-stopped
    env_file:
      - ./backend/.env
    ports:
      - "8000:10000"
      - "10000:10000"
    volumes:
      - ./backend/data:/app/data
